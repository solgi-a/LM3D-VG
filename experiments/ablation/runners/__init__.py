"""One standalone runner per ablation experiment.

Each script is self-contained: it has its own CONFIG block at the top and imports nothing
from the other runners, so editing one experiment can never disturb another.
"""
